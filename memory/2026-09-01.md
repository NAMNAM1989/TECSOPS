# 2026-09-01

## Mobile Ops UI — spec OpenDesign locked

Nhánh `cursor/mobile-ops-ui-redesign-4bfe` từ `main` @ `8cbea84` (sau #74 `f2c8a5c`).

Hierarchy **Ngày → Kho → List → 1 CTA**:

- Chrome: hàng ngày 44px (‹ ngày › Nay · live dot · search) → tab kho label-only cuộn ngang → summary một dòng `Lô · PCS · KG` + chip Tất cả + ⋯
- Lọc / Sheet / Excel / Ảnh → bottom sheet, không thanh cao thứ hai
- Card ~88–104px: accent kho 3px, MAWB · DEST · ⋯, flight · pcs/kg, khách. Không dropdown Volume/Nhận trên card (đổi status trong edit sheet)
- FAB + 56, `right: 16`, `bottom: nav 52 + safe-area + 16`; list pad 148 + safe-area
- Bottom nav 52 + `max(12px, safe-area)`; active primary

Giữ DayPulse logic, DIM, print/CSD, #71 ảnh, Sheets, 4 kho, AppAuthGate, desktop command bar. Không restore Ext/eCargo.
