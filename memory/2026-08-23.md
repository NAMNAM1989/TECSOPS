# 2026-08-23

## A2 — gỡ legacy Ext TECS-TCS (`chrome-extension/`)

Base: `main` @ `41565b3` (#42 Ext-first đã merge).

Đã xóa thư mục `chrome-extension/` + gói catalog `tecs-tcs` + ZIP `tecsops-chrome-extension.zip`.
Menu «Tải Ext» / `/api/chrome-extensions` chỉ **TCS + SCSC**.
`/api/tcs-extension` alias sang Ext TCS.
Giữ: warehouse dữ liệu `TECS-TCS`, handshake #42 (`EXT_READY`, chip, «Đăng Nhập TCS»), Ext TCS/SCSC. A3 (PR này) gỡ `tcs-awb-automation` / `TCS_AGENT_*`.

## A3 cleanup — Gemini + Sheet import + portal-worker

- User xác nhận không còn dùng Gemini AI reporting / nhập lô Google Sheet.
- Đã gỡ UI + `server/ai` + `/api/sheets` (BOOK Hằng Ngày). File Drive/Sheets của user không đụng.
- `applySessionIdOrder` giữ trong `server/sessionOrder.mjs` cho `REORDER_SESSION`.
- Gỡ `portal:worker*` / `deprecated-portal-remote` / `portal-worker.mjs`. Dual-agent gỡ ở A3 (PR teardown).
- Railway có thể xóa `GEMINI_*` sau merge.
- Không gỡ: chrome-extension, TCS_AGENT, eCargo/Ops/Customers/Stats/print/CSD/DIM.

## Wire UI `synced_at` SoT (namnamlogistics)

- Cột đã có: `lots.synced_at`, `ops_customers.synced_at` (timestamptz, 0 null).
- Không migration. Overlay `/api/state` + `GET /api/sync-meta`.
- Ops strip = max lots theo kho đang xem + ngày phiên; Customers strip riêng.
- Format Asia/Saigon; null → ẩn timestamp.

## Rebase PR #42 lên `main` @ `4511d20`

- Nhánh `cursor/ext-ops-protocol-status-ef32` rebase lên latest main (#41/#45/#46/#47/#49/#50/#51).
- Conflict: `package-chrome-extension.mjs` (giữ deprecation TECS-TCS + protocol TCS), `EcargoScscInlineBar.tsx` (giữ chip Ext, bỏ `trackAiEvent` vì A3 gỡ Gemini), `memory/2026-08-22.md` (giữ A2 + ghi chú #42).
- Auto-merge: `TcsPortalInlineBar`, `useTcsPortalActions`, `server/index.mjs`, `docs/railway-online-portal.md`.
- Không gỡ Ext TCS/SCSC; copy «Đăng Nhập TCS»; chưa teardown TECS-TCS / Python agent.

## A3 — teardown Railway/Python dual-agent

Base: `main` @ `f6f7e56` (#52).
Xóa `tcs-awb-automation/`, spawn/proxy scripts, npm `tcs:agent*` / `portal:start:*` / `railway:setup-portal`.
Dockerfile lean Node. `/tcs-agent` stub 410. Policy chỉ Ext.
Bugbot #52: `tcsExtChannelForWarehouse("TECS-TCS")` → `tecsops-tcs-direct-ext`.
Giữ mã kho `TECS-TCS` / `TECS-SCSC`. Railway follow-up: gỡ `TCS_AGENT_*` + volume `browser_profile`.

## Hotfix Docker OCR (sau #53 fail trên Railway)

`93549ee` slim Dockerfile Node-only nhưng vẫn `npm run ext:fetch-ocr` → thiếu Python/ddddocr → thiếu `common.onnx` (gitignore) → build fail.
Prod vẫn A2 `f6f7e56`. Env/volume chưa gỡ.

Fix: Dockerfile multi-stage (python extract wheel ddddocr==1.5.6 → node builder fetch-ocr+build → node runtime). Không dual-agent / Playwright / TCS_AGENT spawn. `prebuild` vẫn đóng ZIP TCS+SCSC; Docker set `EXT_OCR_REQUIRED=1`.

## A4 — portal UI Ext-only (WIP)

Base: `main` @ `88b2498` (A3 + Docker OCR hotfix).
Gỡ chồng chip / «Trực quan» / CTA login giả trên mobile.
`TcsPortalInlineBar`: chip Ext (offline / sẵn sàng / đã login) + «Đăng Nhập TCS» + overflow Quét/Điền/PDF.
`PortalExtStatusChip` + `portalBarUi` dùng chung với SCSC.
Mobile: «cần Ext trên PC» — không Đăng Nhập TCS giả.
Không đụng protocol Ext, không restore agent, mã kho TECS-TCS / TECS-SCSC giữ.

## B1 — cleanup portalJobs / tcs-agent / hook chết

Base: `main` @ `ecf5105` (#55 A4 Ext-only).
Xóa `server/portalJobs*` + `/api/portal-*`, stub `/tcs-agent` (frontend không còn gọi).
Gỡ `visualControl` / `playwrightLocal` / `scanReceptionWithAgent` / `preferRemotePortal` / `refreshPortalWorker`.
Ext TCS 1.5.3 + SCSC 1.0.3: bỏ host `127.0.0.1:8765/8766`. OCR CAPTCHA chỉ ONNX trong Ext.
Giữ: Ext TCS/SCSC, Docker OCR #54, eCargo/IMAP OTP, print/CSD, mã kho TECS-TCS / TECS-SCSC, UI chip + «Đăng Nhập TCS».
Không: B2 docs/mcp, RLS, migration, dual-agent.

## B2 — docs/mcp + leftover policy (sau #56)

Base: `main` @ `249af1a` (B1).
Xóa SAFE: `TECSOPS-*-AUDIT*`, script PDF redesign checklist, `mcp/tecsops-deploy/`, `portalExecutorPolicy` (không import sau B1).
Giữ REVIEW: blueprint/proposals/spec/prompt 12/08, checklist HTML, `ext_tcs-analysis`, Card.tsx, downloads HTML.
Không đụng Ext/OCR/eCargo/print/CSD/auth/DB. CTA vẫn «Đăng Nhập TCS».
