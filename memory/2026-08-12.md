# 2026-08-12

## Parity TECS-TCS vs TCS (Playwright MCP)

- UI menu ⋮ **giống nhau**: Điền + Tải PDF ESID trên cả 2 kho; toolbar đều có ĐN + Quét.
- Nguyên nhân TCS “hạn chế”: agent `:8766` **không chạy** (local chỉ spawn `:8765` khi thiếu `TCS_AGENT_DUAL` / `TCS_USERNAME_TCS`+`TCS_PASSWORD_TCS`). Health TCS = `AGENT_OFFLINE`.
- Bug UX: `refreshHealth` giữ `prev.ok` forever → đổi sang kho TCS vẫn hiện «Chờ ĐN / READY» của hub. Đã sửa: reset health khi đổi kho + miss counter (1 ping).
- Đã cấu hình dual local: `.env.local` `TCS_AGENT_DUAL=1` + user hub `hanam7195` / TCS `namnam8012` (từ `.env.hub`/`.env.tcs`). Agent `:8766` online. `dev.mjs` tự spawn cả 2 khi dual bật.
- Recheck parity: UI ĐN/Quét/Điền/PDF giống nhau. Ext message types giống. Lệch còn lại có chủ đích: user/port/ext channel + checkbox form `tecs_warehouse` chỉ TECS-TCS. Sửa `TcsPortalInlineBar`: hiện Cài đặt ESID + panel HOÀN TẤT cả khi compact (trước bị ẩn cho cả 2 kho).
- Phone-only Railway test (390×844, không máy kho/LAN): dual agent cloud OK + đã ĐN (`hanam7195`/`namnam8012`), Quét bật, PDF ESID có trên ⋮. **Điền bị cắt trên mobile** (`!compact`) — đã mở lại trong `ShipmentRowActionsMenu`. Fix local chưa deploy Railway.
- Playwright test Điền kho TCS (Railway desktop): UI có «Điền»; agent `namnam8012` logged_in; `POST /tcs-agent/esid/declare-fill` chạy. **FAIL nghiệp vụ**: không khớp chuyến bay (`no_match`) → 400 «TCS chưa xác nhận và lưu chuyến bay». VD VN0773 date `08-13-2026` nhưng modal list toàn `12AUG2026`. Không HOÀN TẤT.
- Commit/push `upgrade/g0-railway-ci`: `e2fe7b6` portal parity + `5e0b8fc` G0–G5. Fix Vitest node-env cho fs tests + portalJobs memory fallback khi Postgres chết. Gate: typecheck/lint/deploy:check PASS, Vitest 87/547 PASS.
- Railway deploy: merge FF `upgrade/g0-railway-ci` → `main` (`2845ca9`), push origin/main. Lần đầu crash vì thiếu `TECSOPS_APP_TOKEN` (G2 fail-closed). Đã set token trên Railway + lưu `.env.local` (không commit). Redeploy Online: `/api/health` postgres=true, `/api/auth/status` required=true, login OK, dual tcs-agent OK. URL: `https://ops-production-b405.up.railway.app`. Backup preflight: `tecsops-postgres-state-2026-08-12T11-32-14-478Z.json`.
- Playwright MCP Điền kho TCS (Railway, 2026-08-12):
  - **FAIL** AWB `738-0728 7173` VN773 **13AUG**: HTTP 400, `reason=date_picker_failed`, warning «Không chọn được ngày bay '2026-08-13' bằng lịch TCS», `flight_ms≈1.3s`. Warehouse header `X-Portal-Warehouse: TCS` OK, agent đã ĐN.
  - **PASS** AWB `297-3970 6516` CI0784 **12AUG**: HTTP 200, chọn chuyến OK (`CI 0784 12AUG2026…`), Shipper/CNEE/Agent/Hàng OK; cảnh báo Dest TPE + Other Request. Không bấm HOÀN TẤT.
  - Kết luận: kho TCS **không** bị chặn path Điền; lỗi ở `_select_modal_flight_date` khi đổi sang ngày ≠ ngày đang sẵn trong `#flightDate`. Nghi ngờ verify format: form TCS trả `12/08/2026` (DD/MM) trong khi matcher chỉ chấp nhận MM-DD-YYYY / YYYY-MM-DD.
- Retest sâu (Playwright probe API, sau ĐN lại):
  - `#flightDate` modal = `08-13-2026` / `08-12-2026` (MM-DD) → **date picker thường OK**; `date_picker_failed` chỉ intermittent.
  - TCS user: search có `flightNo=VN0773` + `search_clicked` nhưng bảng vẫn **20 chuyến theo ngày** (không lọc số hiệu) → phụ thuộc pagination.
  - **Pagination walker hỏng**: sau click page 2 → `count:0`, page 3 → `no_page_item`/`labels=[]` → dừng sớm, không tới trang có VN*.
  - TECS-TCS hub **PASS** VN570 13AUG (`pick_count=1`, search lọc đúng). TCS CI0784 12AUG **PASS** lại (chuyến đúng nằm sẵn trang hiện tại).
  - VN773-12AUG từng match rồi fail popup Đồng ý (timeout 4.2s) — nhánh confirm cũng flaky.
  - Nguyên nhân chính kho TCS: **search số hiệu không lọc + pagination gãy**, không phải kho bị khóa Điền.

## Playwright MCP comprehensive audit

- Chạy theo `docs/CURSOR_PLAYWRIGHT_MCP_COMPREHENSIVE_AUDIT_PROMPT.md`
- BASE_URL: `http://127.0.0.1:3001` (Postgres Docker local). Mutation BLOCKED vì state ops thật + Live concurrent.
- Artifacts: `output/playwright-audit/20260812-140656/`
- Reports: `docs/TECSOPS-PLAYWRIGHT-AUDIT-2026-08-12.md`, `docs/TECSOPS-UPGRADE-PROPOSALS-2026-08-12.md`
- Baseline khớp: vitest 83/529; pytest combobox still FAIL; lint 2 any warnings.
- P0: unauth `/api/state`+mutation; missing security headers.
- Follow-up: báo cáo tổng hợp triển khai toàn phần → `docs/TECSOPS-MASTER-UPGRADE-BLUEPRINT-2026-08-12.md` (G0–G5: Railway, Playwright, logic, perf, AI).
- Không sửa production; không portal submit; không in thật.

## G0 implementation

- Nhánh `upgrade/g0-railway-ci`; chưa commit/push/deploy.
- Thêm security headers, mutation rate-limit, redaction lỗi production.
- `/tcs-agent/health` offline trả 200 + `ok:false` để không spam console.
- Sửa combobox short-tail PCS; pytest full PASS.
- `qa:smoke` mặc định read-only; opt-in mutation có marker + cleanup ID.
- Lint 0 warning; Vitest 84/534, pytest, typecheck, build, deploy:check đều PASS.
- Production-mode Playwright trên port cô lập: 0 console errors/warnings; health headers và Postgres OK.

## G1→G5 implementation

- G1: thêm Playwright L1 read-only 16/16, L2 create/update/delete + cleanup marker, AI mock E2E; tài liệu `tests/e2e/README.md`.
- G2: production fail-closed với `TECSOPS_APP_TOKEN`; login tạo HttpOnly/SameSite cookie; state/mutation/AI/Socket cùng auth. Offline queue chặn trước apply khi đầy; server Customer Code parity; AWB 11 số unique, AWB dở được phép trùng.
- G3: tách PDF/CSD/DIM/AI khỏi critical path. `AirCargoTracking` từ 1,272.07KB (586.50KB gzip) xuống ~249.54KB (73.67KB gzip).
- G4: AI-1→AI-9 qua schema whitelist, auth/rate-limit, PII-sanitized snapshot, telemetry; UI workbench + Sheet explain + Stats end-day; rule checklist trước Fill/Register; `GEMINI_DISABLED=1` fail-fast.
- G5: axe 0 critical Ops/Customers/Stats/AI, modal focus trap + Escape trả focus, mobile sticky button ≥44×44px.
- Final: Vitest 87 files/546 tests PASS; pytest full PASS; typecheck/lint/build/deploy:check PASS. Production dependency high đã được vá; còn 2 moderate từ `exceljs→uuid` (không force downgrade).
- Chưa commit/push/deploy. Không mutation DB Ops; L2 dùng Postgres Docker riêng `tecsops-g2-e2e-postgres`, sau test phải dừng/xóa container.

## Portal toi uu cloud (implement)

- Gate UI bang `isMobile` (KHONG dung `compact`) — desktop header van `compact` nhung con nut DN.
- Policy `auto`: desktop Ext→agent; mobile agent-only (Quet/PDF). An Dien + HOAN TAT tren phone.
- Quet agent: `POST /workspace/scan` (khong prefetch/warm). `TCS_PDF_PREFETCH_N` default 0.
- Volume: `prune_docs` (`TCS_DOCS_RETENTION_S` 48h) luc boot + sau DOWNLOAD; prune Chrome Cache khi `session.close`.
- Ops load: `GET /api/state?sessionDate=` / `?full=1`; socket scoped sync; Stats dung full.
- Bay: nham `compact` = phone se xoa DN tren PC.

## Agent master fill accuracy

- Agent (#agentId): tim MST/VAT + S�T truoc ten; min_score=72; verify label sau chon; bo chon neu diem thap / top-2 ambiguos.
- Scoring: reject Them moi; can >=60% rare tokens; phat option thua token; hint VAT = 98.
- Ext v1.4.5 / script 2.0.28 dong bo. Pytest combobox 7/7 PASS.
- Can reload Ext + restart/redeploy agent de co hieu luc.

## Skip Notify + Other Request on fill

- Khong dien #notifyId va textarea Other Request (otherRequest/shcOthReq).
- Payload fill + agent service ep rong; Ext/Playwright clear 2 o. Excel van giu core.
- Ext v1.4.6 / script 2.0.29.
