import type { CustomerDirectoryEntry } from "../types/customerDirectory";

/** Danh bạ mặc định — trống (không seed demo). */
export function buildDefaultCustomerDirectory(): CustomerDirectoryEntry[] {
  return [];
}
