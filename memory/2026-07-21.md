# 2026-07-21

## ESID: xem form + HOÀN TẤT từ Ops (máy khác)

- Điền chạy trên agent → không mở được tab TCS cá nhân đã điền.
- Sau `declare-fill`: screenshot → `preview_file` / `GET /docs?file=` (PNG được phép).
- `POST /esid/declare-submit` + `confirm_submit: true`: verify AWB form → tick agree → HOÀN TẤT.
- Ops: panel ảnh preview + nút «HOÀN TẤT trên TCS» (confirm) trên thanh TCS.
- Điền vẫn mặc định không tự submit.

## ESID headed máy kho (hướng 1)

- Local/`npm run dev`/`tcs:agent:real`: mặc định `TCS_HEADLESS=0` — Chrome thật.
- Railway `start-fullstack`: mặc định `TCS_HEADLESS=1`.
- Sau Điền: `focus_window` + Ops nút «Hiện Chrome»; kiểm tra trên máy kho rồi HOÀN TẤT Chrome hoặc Ops.
- `POST /session/focus`; health trả `headless`.

## Login hiện Chrome TCS thật

- `POST /session/open` + `visible:true` (nút Login Ops): force headed, hiện ESID/Agent, focus cửa sổ.
- Quét/Điền/Submit/Prepare/Job: `focus_if_headed` trước thao tác.
- Launch: thử Chrome channel → Chromium bundled (Railway không có `/opt/google/chrome`).
- Railway: headed fail → fallback headless + ảnh `TCS_LOGIN_PREVIEW_*.png` trên Ops (một lần, không poll).
- Thao tác từ máy khác: **TCS desktop** (noVNC), không dùng xem ảnh live.

## ESID Người khai / Agent dùng chung mọi máy

- Lưu trong `app_state` Postgres: `esidRegistrantStore`, `esidAgentStore`.
- Mutation `SET_ESID_REGISTRANT_STORE` / `SET_ESID_AGENT_STORE`; hydrate localStorage khi `/api/state`.
- Máy có dữ liệu local mà server trống → tự đẩy lên lần đầu.

## noVNC TCS desktop (Railway)

- Docker: `xvfb` + `fluxbox` + `x11vnc` + `novnc`/`websockify`; script `scripts/start-tcs-desktop.mjs`.
- `start-fullstack`: `TCS_VNC=1` → desktop rồi agent `DISPLAY=:99` `TCS_HEADLESS=0`; `TCS_VNC=0` → headless.
- Express `server/tcsDesktopProxy.mjs`: `/tcs-desktop` HTTP+WS → `127.0.0.1:6080`.
- Ops nút **TCS desktop** → noVNC (đã gỡ panel «Xem ảnh» live / poll screenshot).

## Audit cleanup (2026-07-21)

- Baseline: typecheck/lint/282 tests/build OK.
- Đã xóa dead FE: SummaryBar, printBrowserLabel, customer sticky/save orphan, thermalLabel dead cluster, printMmUnits, probe-google-sheet.
- Gỡ export chết: downloadTcsPortalJobJson, formatYmdToVnDisplay, focusAgentBrowser UI path.
- Round 2: xóa cụm printer localStorage (`printerProfiles`/`Storage`/`Migrate`/`Sync`/`index`/`labelDimensions`), `report_service.py`, `focusTcsAgentSession` FE, StatusReadonly + status style chết, party copy-block, compact HAWB helpers, v.v. CSS gzip −1KB (purge Tailwind).
- Đã xóa tooling tay: `tcs-awb-automation/scripts/live_*.py`, `debug_*`, `diagnose_*`, `scripts/reset-trial-data.mjs`, `import-customers-xlsx.ts`, `verify-day-excel-count.mts`.
- Chưa đụng: `print_service.py` (còn test), hợp nhất AWB/date helpers.
- Railway: `TCS_VNC_PASSWORD` tùy chọn (trống = `-nopw`); volume `TCS_BROWSER_PROFILE`.
