# Daily Memory - 2026-06-07

## Những việc đã làm hôm nay (Redesign & Cleanup)

1. **Loại bỏ tệp tin & mã nguồn dư thừa**:
   - Xóa bỏ thành công các thành phần liên quan đến `WeighSlipManager` cũ: `PrintCalibrationModal.tsx`, `WeighSlipManager.tsx`, `useWeighSlips.ts`.
   - Xóa các route backend và schema PostgreSQL tương ứng: `weighSlipRoutes.mjs`, `weighSlipStore.mjs`, `weighSlipSchema.mjs`.
   - Gỡ bỏ import và đăng ký route cũ trong `server/index.mjs` và dọn dẹp `server/postgresStateStore.mjs`.
   - Loại bỏ các tệp mồ côi `applyCustomerToWeighSlipDraft.ts` và `weighSlipValidation.ts`.

2. **Nâng cấp giao diện Mobile**:
   - Thiết kế thanh hành động nổi `StickyMobileActions` thành dạng Pill Menu lơ lửng, dùng nền kính mờ HSL sang trọng và bo tròn góc lớn.
   - Nâng cấp các warehouse header accordion thành các block bo tròn mượt mà, hỗ trợ xoay chevron và đếm badge.
   - Thêm hiệu ứng trượt trơn tru từ đáy màn hình (`animate-cnee-slide-up`) cho `MobileShipmentEditSheet` và chuyển đổi tab thành dạng Segmented Control.

3. **Nâng cấp giao diện Desktop & Main Layout**:
   - Chuyển đổi header trang chính thành kính mờ dính đầu trang (`sticky top-0 z-40 backdrop-blur-xl border-b border-black/[0.04]`).
   - Cải tiến nút Dark Mode có micro-animation xoay icon và hiệu ứng nhấn scale-90.
   - Thiết kế các hàng dòng trong bảng `DesktopShipmentTable` thành dạng thẻ bo góc mềm mại, thêm hiệu ứng di chuột nổi bật dòng (`hover:-translate-y-0.5`).
   - Tối ưu màu sắc trạng thái mềm mại, dịu mắt và thêm chấm LED phát sáng (glow) cạnh trạng thái.

4. **Xác minh & Hoàn tất**:
   - Chạy Vitest thành công: **349/349 tests passed**.
   - Biên dịch sản phẩm thành công: `npm run build` hoàn tất không lỗi.
