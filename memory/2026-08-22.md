# 2026-08-22

## Research Ext TCS (chrome-extension-tcs)

- Baseline `main` `dd1de48` (sau #45). Không có folder `ext_tcs`.
- Ext khuyến nghị App-click kho TCS: `chrome-extension-tcs` (channel `tecsops-tcs-direct-ext`).
- Protocol `docs/ops-ext-protocol.md` chỉ trên PR draft #42 (`cursor/ext-ops-protocol-status-ef32`), chưa merge.
- Ops `main` nuốt `EXT_READY`; «Trực quan» + Ext offline vẫn fallback agent Railway.
- Phân tích: `docs/ext_tcs-analysis.md`. Không xóa credential, không refactor lớn.

## Cleanup Step A2 — docs weight (không mất data)

Base: `main` @ `e407fc1` (#48 assets + #47 `docs/ext_tcs-analysis.md`).

Đã xóa (verify: không import runtime, `npm run ci` không đọc):

- `docs/audit-baselines/*.png` — 8 ảnh visual QA, chỉ nhắc trong `TECSOPS-REDESIGN-AUDIT.md` — **527 923 B**
- `docs/tecsops-ui-redesign-checklist.pdf` — trùng HTML — **227 283 B**
- `TECSOPS-Production-Readiness-Report.docx` — không reference — **50 068 B**

**Tổng:** **805 274 B (~786 KiB)**

Giữ: `docs/ui-review.md`, `docs/ext_tcs-analysis.md`, railway/otp, checklist HTML, `public/fonts`, `public/brand`, CSD runtime, chrome-extension*, `tcs-awb-automation`.

Không chắc (không xóa): 3 file `*2026-08-12.md`, `TECSOPS-REDESIGN-AUDIT.md`, spec redesign, prompt Playwright MCP, script generate PDF, `docs/air-cargo-label-*.html`.
