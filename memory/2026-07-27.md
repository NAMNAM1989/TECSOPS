
# 2026-07-27

## TCS ESID UI cleanup
- Gỡ «Sửa tay (noVNC)» và «URL agent» khỏi toolbar TCS.
- Tải Chrome Ext qua `/api/tcs-extension` → ZIP `tecsops-chrome-extension-v{version}.zip` (hiện v2.0.11).
- Nút «Nhập Sheet» hiện rõ desktop + mobile (`OpsSheetImportButton`).
- Fix tải PDF ESID: mất user activation sau job dài → iframe + Content-Disposition; proxy bỏ hop-by-hop khi có Content-Length.


## Gỡ noVNC / TCS_VNC
- Xóa server/tcsDesktopProxy.mjs, scripts/start-tcs-desktop.mjs.
- start-fullstack + Dockerfile chỉ API-first headless; giữ TCS_HEADLESS.
- Gỡ dep http-proxy; docs TOOLS/README/railway cập nhật.


## Dong bo TCS 1 lan
- Nguyen nhan: nut Dong bo gui TK/MK rong + Ext ping chua kip → lan 1 fail/Playwright, lan 2 moi login.
- Fix Ops: syncTcs refreshExtension (+retry), gui credential tu form; Ext v2.0.14 cho CAPTCHA san sang truoc OCR.

## PDF ESID toc do
- Khong phai 2 luong Ext+Playwright cung tai PDF (chi Playwright).
- Bottleneck: moi click IN + networkidle 8s + FE fetch 2 lan + excel/delay.
- Fix: bo networkidle, cache PDF 10p, skip excel/delay/focus, FE 1 lan fetch.

## PDF speed experiment
- Bench AWB 23219556213: cold print 5.41s; cache hit 0.03s.
- Fast HTML + scratch page + sticky detail + TTL 8h.
- Force bypass cache (force=true) de test in lai.

## PDF speed round 2
- Prefetch sau bootstrap (TCS_PDF_PREFETCH_N=5) + Ext goi /workspace/prefetch-pdfs.
- Isolate iframe print (bo serialize khi duoc); frame detect 1 evaluate.
- Search wait 7s; timing breakdown TCS_PDF_TIMING=1.

## Dong bo Ext khong tu login
- Root cause: agent chay system Python312 thieu `ddddocr` → `/captcha/solve` OCR_FAILED → Ext khong auto login.
- Fix: `spawnTcsAgent` chi dung Python co ddddocr (.venv); health `captcha_ocr`; Ext v2.0.15 local-first OCR + xu ly OCR_FAILED; harden setNativeValue React.
- Verify: health captcha_ocr=True; solve dummy → 422 OCR_EMPTY (khong con 500 module missing).

## Fix race Dong bo vs Tai PDF + Ext 2.0.17
- Agent: acquire_running cho job (cho toi 120s) thay vi 409 BUSY ngay.
- FE: agentWarmupRef sau Ext sync; downloadEsidFor await + retry BUSY.
- Ext 2.0.17: inject theo version, content listener luon reply + safe sendResponse.

## Ultimate bug-fix (repo-wide)
- B01: chặn `RESET_TRIAL_DATA` trên production trừ `ALLOW_RESET_TRIAL_DATA=1`.
- B03: Sheet patch không ghi `dimWeightKg: null` → giữ DIM đã đo trên Ops.
- B04: ADD gọi `migrateShipmentStatus` (server + client).
- B05: `TCS_AGENT_PROXY` mặc định tắt khi `NODE_ENV=production` (Docker vẫn set `=1`).
- B02/B07: retry `/api/state`, reconnect khi `online`, `saveRows` chỉ khi state mới hơn.
- B06: persist airline overrides localStorage khi online sync.
- Validation: typecheck + 335 tests + lint + build + deploy:check PASS.
- WIP label/AWB của user được giữ nguyên (không đụng).
