# 2026-08-13

## Hướng 5 — OCR CAPTCHA trong Chrome Ext (ONNX)

- Triển khai OCR offline trong Ext TCS **1.5.0** + TECS-TCS **2.6.0**:
  - Offscreen MV3 + `onnxruntime-web` + `ddddocr` `common.onnx`
  - `solveCaptcha`: Ext OCR → agent localhost → CAPTCHA tay
- Script: `npm run ext:fetch-ocr` (pip `ddddocr` + dep `onnxruntime-web`)
- Binary lớn gitignore; ZIP package ~67MB gồm model
- Docs: `docs/railway-online-portal.md` (mục CAPTCHA)
- PC ĐN không còn phụ thuộc agent/cloud cho CAPTCHA nếu Ext đủ asset

## Fix Ext kho TCS lỗi (ZIP thiếu OCR)

- Nguyên nhân: Railway `prebuild` không chạy `ext:fetch-ocr` → ZIP tải từ Ops ~234KB thiếu `ort.min.js`/`common.onnx` → offscreen OCR vỡ khi ĐN.
- Sửa: Dockerfile `npm run ext:fetch-ocr && npm run build`; fetch-ocr copy onnx không cần `get_charset`; Ext skip OCR nếu thiếu binary.
- Ext TCS **1.5.1** / TECS-TCS **2.6.1** — tải lại ZIP (~67MB) rồi Reload.

## Playwright — ĐN máy không Ext + Điền đúng kho

Nguyên nhân ĐN TCS cloud/máy khác hỏng:
- UI khóa «Trực quan» cả khi Ext offline → hiện form Ext, không gọi agent.
- Agent tin marker `.tecsops_login_user` + URL `/Awb/Agent` lúc redirect → bỏ OCR; `_ensure_login` luôn `return True`; headless ghi đè message OCR.
- HOÀN TẤT từng gửi theo tab đang chọn, không theo kho đã Điền.

Sửa:
- `TcsPortalInlineBar` / policy: chỉ lock Ext khi Ext **online**.
- `useTcsPortalActions`: `ensureSessionReady(warehouse)`, preview + HOÀN TẤT dùng kho đã Điền.
- `session_manager`: chờ UI thật; marker không đủ; về AwbLogin rồi OCR; `return ok`; giữ `NEEDS_LOGIN`.

Playwright local (Chrome MCP, không Ext) 13-AUG:
- Proxy `X-Portal-Warehouse` TCS→:8766 / TECS-TCS→:8765 đúng user `namnam8012` / `hanam7195`.
- Đóng session TCS → UI «Chờ ĐN» → bấm ĐN (TQ bật, không Ext) → «Agent cloud đã đăng nhập (TCS)», không form Ext.
- Điền AWB `738-07287184` VN773 14AUG kho TCS: header `TCS`, `ok`, **chưa HOÀN TẤT**, 13.8s.
- Đổi tab TECS-TCS: toolbar `Cổng TCS · TECS-TCS`, health header `TECS-TCS`.

Railway production chưa có fix agent này — cần deploy để máy khác trên URL prod hết lỗi.

## Mobile — AWB bị che

- Card phone: AWB chung hàng với `<select>` status. iOS phình select theo «Hoàn thành tiếp nhận» → đè số AWB (`truncate`).
- Sửa: AWB hàng riêng (`formatAwb`, không truncate); status nhãn gọn (HT/Nhận) + khung `4.75rem overflow-hidden`; HAWB dòng phụ.
