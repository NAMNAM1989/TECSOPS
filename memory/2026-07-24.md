# 2026-07-24

- Rà soát deployment: Railway production `ops-production-b405.up.railway.app` đang chạy deployment `6aee203d-8cfc-4766-9606-1732c177c4a9`, tạo lúc 2026-07-24 03:12:28Z bằng CLI upload local (`reason: deploy`, không có `commitHash`).
- Bundle production trùng chính xác build từ worktree local `D:\TECSOPS` (`page-print-nZgW_1ZZ.js`), không trùng build của `origin/main` commit `8ac5f80` (`page-print-CU4ZqyZ1.js`).
- Commit `8ac5f80 improve thermal label printing` từng được Railway deploy/redeploy nhưng các deployment đó đã ở trạng thái `REMOVED`; lần upload local sau cùng đã ghi đè, làm production quay về luồng in tem cũ.
- `main` local đang phân kỳ với `origin/main`: ahead 3, behind 37 và còn nhiều thay đổi chưa commit. Không deploy trực tiếp từ worktree này trước khi hợp nhất/chốt nguồn chuẩn.
- Cả snapshot production local và worktree `8ac5f80` đều qua lint, build, tests và deploy safety check; production snapshot: 45 files/285 tests, label worktree: 49 files/284 tests.
- Đã tạo nhánh tích hợp `codex/railway-label-release` từ `origin/main`, đưa 3 commit local + 12 thay đổi local + fix `python3` vào, giải quyết 2 xung đột test và push commit phát hành `0f8addf`.
- Backup trước deploy: `backups/tecsops-postgres-state-2026-07-24T03-42-49-496Z.json` (rows=290, version=786, bytes=331177).
- Railway deployment `74f28fdd-ba75-40d8-8704-707e3b3b6d8e` đạt `SUCCESS`. Production health 200/Postgres true, asset hashes trùng build release và bundle `page-print-CU4ZqyZ1.js` chứa đầy đủ marker giao diện in tem mới.
