# 2026-09-04

- Thêm **H21 TCS** độc lập với H21 SCSC: catalog/stamp Postgres `tcs_h21_*`, API `/api/tcs-h21/*`, trang `#/tcs-h21`, invoice modal chỉ kho `TCS`.
- Seed ban đầu trống (`data/tcs-h21/`) — import qua UI. Migrate: `npm run migrate:tcs-h21`.
- Phân tích DIM: divisor luôn 6000; làm tròn theo catalog 33 hãng SCSC (flight rồi AWB), không theo kho. QR_LINE code cắt 1dp khác note SOP. Smart fill sinh kiện ước tính dưới kg lô.
- Nâng cao DIM: 4 kênh lưu size chồng; «Sinh ngay»≈«Điền đủ»; áp mẫu wipe bảng; regenerationNonce chưa gắn UI; dimCustomPresetsStorage dead. Đề xuất P0 gộp CTA + toggle kg.
- Modal DIM mở rộng: sm≤72rem / lg≤90rem / xl≤96rem, cao ~96–98dvh; bảng chữ lớn hơn; 1 CTA «Bù kiện ước tính»+Sinh lại (nonce); preset 92/95/98%; khóa kg cố định; footer Nâng cao scroll tới panel.
- Drag DIM desktop: useDraggablePanel — kéo header, clamp viewport, double-click về giữa; mobile sheet không kéo.
- Blueprint highest-tier DIM: Wave A QR SOP + estimated export + unify storage; Wave B undo/keyboard/template; Wave C drag+/server sync; Wave D VOLUME_DONE policy.
- Wave A (A1–A3) done: QR_LINE theo SOP (0 giữ / 1–4→0.5 / ≥5→nguyên); Excel+print cột NGUỒN Đo/Ước; copy suffix `*`; Lưu DIM ghi history; nút «+ Ghim gần đây»; custom presets deprecated + vẫn hiện legacy.
- Wave B (B1–B4): undo/redo 10 bước (Ctrl+Z/Y); keyboard grid Enter/↑↓ + Ctrl+Enter lưu + Ctrl+B bù + Ctrl+G gộp; mẫu Thay/Chèn/Scale + auto defaultDimTemplateId; preset 1-click commit; Suspense skeleton.
- Wave C (C1–C3): drag+ persist/resize/Ctrl+nudge + visualViewport clamp; mẫu DIM multi-line sync hồ sơ khách (server); draft autosave theo shipmentId + cảnh báo khi pcs/kg đổi.
