# 2026-08-26

## Sheet — sai ngày phiên khi kéo dữ liệu

User: bấm kéo dữ liệu về thì lệch ngày (hàng hôm nay phải lấy tab hôm nay).

Nguyên nhân:
- URL/localStorage giữ `gid` tab ngày khác
- Server fallback đúng tab theo `sessionDate` nhưng API sync **echo lại gid request**
- Client «chuẩn hóa URL» lại lưu gid lệch → lần sau dễ kéo nhầm

Sửa (nhánh `cursor/fix-sheet-session-date-4a7a`):
- Sync trả `sheetGid` = gid **đã resolve** khớp ngày phiên (không echo gid cũ)
- Client không gửi gid từ URL khi kéo — chỉ theo ngày phiên Ops
- Apply resolve tab theo session (+ tên tab), không theo gid URL
- Lưu URL sạch theo gid đã khớp / bỏ gid nếu mismatch
