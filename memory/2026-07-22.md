# 2026-07-22

## Gom code trùng chức năng (merged PR #7)

- `esidDeclareFields` — nguồn chung Excel dry-run + Điền fill (payment = Chuyển khoản; HAWB thống nhất)
- `esidProfileStoreFactory` — Agent/Người khai dùng chung load/save/normalize
- `downloadXlsx` — blob download + `awbForFilename`
- `customerSavedListOps` — CRUD saved shipper/cnee/goods/vehicle
- `shared/customerProfileLimits.mjs` — limits 1 nguồn cho validate server + clamp client
- Xóa stub `downloadReady` trong `useTcsPortalActions`

## Wave 1 cleanup dead/legacy (merged PR #8)

- Gỡ prop `onOpenTcsPortal` + focus TCS dead (`tcsFocusShipment` / `clearFocusHint`)
- Xóa deprecated SCSC rounding aliases + `scscRuleLabel` + `dimKgExcelNumFmt`
- Xóa `splitScscAddress*` (chỉ test)
- Xóa alias Ecargo vehicle / `loadWorkDate`+`saveWorkDate` exports / `LegacyLabelPrintMode`
- Xóa nhánh `capRatio` 90% trong `dimBulkFill` + `mergePrinterProfileCatalogs`
- Tests: 284 passed

## Wave 2 / 3 (PR #9 / #10 — còn mở)

- #9: ESID UI / print iframe / airline normalize
- #10: Ecargo chết / thermal presets / load locators.json

## Wave 4 (PR)

- `shared/esidProfilesNormalize` — 1 nguồn server + client factory
- `shared/vehiclePlateNormalize` — cho phép `;` (khớp clamp/server)
- `shared/primitiveNormalize` — clamp/str/num printer
- Locator: Python DEFAULT thêm other_request; chrome dual-try otherRequest|shcOthReq
- typecheck/lint OK; tests 286 passed

### Fix bug (cùng ngày)

- PR #9: ESID settings badge/title theo hồ sơ đã lưu; Đóng khôi phục draft
- PR #11: `isValidLicensePlate` đếm chữ/số (không chấp nhận `;;;;`)
- Ext v1.1.3: gộp `ensureLocators` + dual-try other_request (tránh mất khi merge #10+#11)

### Còn lại (rủi ro / cần live DOM)

- Gom full printer normalize (force vs fallback mm)
- Rewire Python `esid_declare_page` qua `_cfg()` / bỏ hardcode party_map
- Xác nhận live DOM id other_request rồi bỏ dual-try
