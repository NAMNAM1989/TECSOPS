# 2026-08-11

## Ext-only + Playwright MCP (không máy kho)

Đã triển khai phương án vận hành chỉ Chrome Ext:

- Policy `auto`/`ext-only` → chỉ `extension`; remote bỏ qua.
- Hook/UI: không portal-worker; PDF/ĐN/Quét mặc định Ext; tắt warmup/prefetch agent.
- Ext CAPTCHA: OCR fail/offline → điền user/pass, `CAPTCHA_REQUIRED` nhập tay.
- Docs QA: `docs/playwright-mcp-ops-qa.md`.
- Deprecate `portal:worker` / `portal:start:warehouse` (exit 1); legacy `*:legacy`.
- Ext versions: TECS-TCS 2.5.3, TCS 1.4.3.

## Online Railway Playwright (không máy kho)

- Policy `auto` = agent cloud → Ext.
- Ops UI ĐN/Quét/Điền/PDF ưu tiên `/tcs-agent` trên Railway.
- Dockerfile: `TCS_AGENT_DUAL=1` + profile paths; docs `docs/railway-online-portal.md`.
- Railway đã setup: Variables dual agent + password từ `.env.hub`/`.env.tcs`;
  volume `browser_profile` dùng subdir `hub/` + `tcs/`; redeploy `railway:up`.
  Script: `npm run railway:setup-portal`.

## QA Playwright MCP — kho TCS phiên 10-AUG-2026

- MCP browser không có Chrome Ext → tạm `agent-only` để E2E ĐN/Quét/PDF.
- ĐN agent TCS (namnam8012): OK.
- Quét: lỗi `#search-form_dateSearch` không visible sau KHAI BÁO/PDF → vá
  `_ensure_list_search_form` / `_click_list_tab` trong `esid_page.py`; restart :8766.
  Sau vá: `scan_ok=true`, ~10 dòng / 6 HT ngày 10/08.
- PDF UI: `297-39706494` và `738-06013335` tải OK (cache).
- UI bar: Quét bị disable khi không Ext dù agent-only → đã nới khi policy agent-only.
- Nhớ: sau QA xóa `localStorage tecsops-portal-executor-policy` về mặc định Ext.
