# 2026-08-25

## Ảnh báo cáo lô — nút trên thanh công cụ

User muốn Vantage / Tecs / TCS / SCSC ra khỏi menu ⋯ (mobile production đang chôn 4 loại chụp ảnh báo cáo).

Nhánh `cursor/ops-cargo-report-toolbar-4a7a` từ `origin/main` @ `8075071`.

- Component mới `OpsCargoReportToolbar`: desktop hàng compact, mobile chip ≥44px cuộn ngang.
- Desktop: bỏ overflow «Báo cáo»; hàng Ảnh nằm giữa command row và DayPulse — không tranh `+ Booking`.
- Mobile: hàng Ảnh sau DayPulse; ⋯ chỉ còn Tải Ext / Công cụ (Sheet, Thống kê, Khách, Excel…).
- Logic copy ảnh / phạm vi kho giữ nguyên (`cargoDayReportImage`, `listOpsCargoReportActions`).
- Không đụng Ext protocol, auth, DIM, print, hash route.

## Hotfix — revert PR #72 (Ext / Đăng Nhập TCS / eCargo)

Railway live `83b839c` (~12:22 ICT) — page + `/api/state` + auth OK, nhưng Ext/eCargo gãy: `SET_ECARGO_SCSC_STORE` unknown action; 404 `/api/ecargo/vct`, `/api/tcs-extension`, `/api/tcs-extension/status`, `/api/chrome-extensions`, `/api/ecargo-extension`.

Nguyên nhân: PR #72 FF-merge `13b756e` + leftover `83b839c` (~−17k dòng).

Revert **chỉ #72** trên `cursor/revert-pr-72-7699` từ `origin/main` @ `83b839c`:
1. `git revert 83b839c` → `1ce048b`
2. `git revert 13b756e` → `8291fa0`

Cây `HEAD^{tree}` = `60253f1^{tree}`. #71 (ảnh báo cáo) vẫn ancestor. Không đụng PR #70. Không reset về `8075071`.
