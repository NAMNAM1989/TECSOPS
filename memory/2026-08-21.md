# 2026-08-21

## Railway cost — không mở Chromium lúc boot

Làm theo `e:\MẪU LÁT XÓA\PROMPT-CURSOR-CHIC-COST.md` trên branch `fix/railway-no-chromium-boot`.

- Dockerfile + start-fullstack: `TCS_AUTO_OPEN=0`, `TCS_AGENT_DUAL=0`. Dual chỉ khi DUAL explicit 1/true/on — không suy từ user/pass TCS.
- Agent chết không kéo sập web. `GET /health` chỉ snapshot, không gọi Playwright.
- Poll `/tcs-agent/health` 15s chỉ sau Đăng Nhập TCS / Quét / session mở; dừng khi AGENT_OFF hoặc idle 2 phút.
- Lựa chọn `TCS_AGENT_ENABLED`: mặc định **1** (vẫn spawn HTTP agent, không Chromium) để Đăng Nhập TCS on-demand còn chạy. `0` = không Python, proxy trả `AGENT_OFF`.
- Không đụng Railway Variables mật khẩu.
