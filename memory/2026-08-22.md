# 2026-08-22

## ERROR_MONITOR_AGENT

Thiết kế + implement agent quan sát lỗi TECSOPS (nhánh `cursor/error-monitor-agent-1098`).

- Runtime: `server/errorMonitor/` — fail-isolated, không sửa business source.
- Queue Bug Fix: `server/data/error-monitor/outbox/` (BUG_FIX_AGENT sibling chưa land contract).
- Hook Express 5xx + `/api/health` 503.
- Tests A–H trong `ErrorMonitorAgent.test.mjs`.
- Docs: `.agents/error-monitor-agent/`
