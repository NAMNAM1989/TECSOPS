/**
 * Kích thước tem vật lý (chiều ngang × dọc khi đọc chữ đứng).
 * Đổi LABEL_HEIGHT_MM = 150 nếu dùng tem 100×150 mm (TSPL SIZE + CSS @page phải đồng bộ).
 */
export const LABEL_WIDTH_MM = 100;
export const LABEL_HEIGHT_MM = 80;
/** Khe tem (TSPL GAP) — chỉnh theo cuộn thực tế */
export const LABEL_GAP_MM = 2;
/** DPI mặc định khi quy đổi mm → dot trong TSPL */
export const LABEL_DPI = 203;
