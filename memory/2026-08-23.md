# 2026-08-23

## Wire UI `synced_at` SoT (namnamlogistics)

- Cột đã có: `lots.synced_at`, `ops_customers.synced_at` (timestamptz, 0 null).
- Không migration. Overlay `/api/state` + `GET /api/sync-meta`.
- Ops strip = max lots theo kho đang xem + ngày phiên; Customers strip riêng.
- Format Asia/Saigon; null → ẩn timestamp.
