node_modules
dist
coverage
.nyc_output
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.vite
*.tsbuildinfo
server/data/state.json
server/data/*.tmp
.DS_Store
*.local
.env
.env.*
!.env.example

# Backup state (có th? ch?a d? li?u th?t ? không commit nh?m)
backups/
tecsops-state-*.json
**/tecsops-state-*.json
tecsops-postgres-state-*.json
**/tecsops-postgres-state-*.json
tecsops-prod-state-*.json
**/tecsops-prod-state-*.json

# Debug / scratch (không commit)
tmp/
.tmp-*
outputs/
output/
.openclaw/

# Leftover extension ZIPs ? không ship (T?i Ext ?ã g?)
public/downloads/*.zip

# Local scratch
.trash/

# Local TCS automation workspace (không ship trong app Ops)
tcs-awb-automation/

