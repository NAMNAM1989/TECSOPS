# 2026-08-11

## Ext-only + Playwright MCP (không máy kho)

Đã triển khai phương án vận hành chỉ Chrome Ext:

- Policy `auto`/`ext-only` → chỉ `extension`; remote bỏ qua.
- Hook/UI: không portal-worker; PDF/ĐN/Quét mặc định Ext; tắt warmup/prefetch agent.
- Ext CAPTCHA: OCR fail/offline → điền user/pass, `CAPTCHA_REQUIRED` nhập tay.
- Docs QA: `docs/playwright-mcp-ops-qa.md`.
- Deprecate `portal:worker` / `portal:start:warehouse` (exit 1); legacy `*:legacy`.
- Ext versions: TECS-TCS 2.5.3, TCS 1.4.3.

## Online Railway Playwright (không máy kho)

- Policy `auto` = agent cloud → Ext.
- Ops UI ĐN/Quét/Điền/PDF ưu tiên `/tcs-agent` trên Railway.
- Dockerfile: `TCS_AGENT_DUAL=1` + profile paths; docs `docs/railway-online-portal.md`.
- Railway đã setup: Variables dual agent + password từ `.env.hub`/`.env.tcs`;
  volume `browser_profile` dùng subdir `hub/` + `tcs/`; redeploy `railway:up`.
  Script: `npm run railway:setup-portal`.

## QA Playwright MCP — kho TCS phiên 10-AUG-2026

- MCP browser không có Chrome Ext → tạm `agent-only` để E2E ĐN/Quét/PDF.
- ĐN agent TCS (namnam8012): OK.
- Quét: lỗi `#search-form_dateSearch` không visible sau KHAI BÁO/PDF → vá
  `_ensure_list_search_form` / `_click_list_tab` trong `esid_page.py`; restart :8766.
  Sau vá: `scan_ok=true`, ~10 dòng / 6 HT ngày 10/08.
- PDF UI: `297-39706494` và `738-06013335` tải OK (cache).
- UI bar: Quét bị disable khi không Ext dù agent-only → đã nới khi policy agent-only.
- Nhớ: sau QA xóa `localStorage tecsops-portal-executor-policy` về mặc định Ext.

## QA Điền ESID kho TCS (Playwright MCP)

Lỗi khi bấm Điền — tái hiện được:

1. **Gate Ops:** kho TCS ngày 10-AUG chưa có lô «Hoàn thành tiếp nhận» →
   UI báo *Chỉ điền lô đã Hoàn thành tiếp nhận — bấm Quét trước*.
2. **Agent Railway:** status list Đã ĐN nhưng tab `declare` phụ kẹt login →
   `NEEDS_LOGIN`; UI còn che bằng *Cần Ext … online* khi không có Ext.
3. **Sau login recovery:** modal chuyến bay OK nhưng lịch `#flightDate` fail
   (title/format) → *Không chọn được ngày bay*.

Sửa local (chưa deploy Railway):

- `session_manager.workspace_page(recover_login=True)` + fallback tab list.
- `esid_declare_service` recover declare page.
- `choose_flight` chờ modal theo `#flightNo`; date picker JS + title đa dạng.
- `useTcsPortalActions`: không che lỗi agent bằng SKIP_EXT.
- Test: `tests/test_workspace_page_recover.py`.

Verify local `:8766` AWB `29739706494` / CI0784 / 2026-08-10 → `ok:true`,
chọn chuyến + Đồng ý OK (warn agent master / AWB đã đăng ký).

## Điền ESID độc lập khỏi Quét HT (chốt)

- **Quét**: chỉ cập nhật status HT trên Ops (đối soát list TCS).
- **Điền**: tạo phiếu khai báo ESID — chỉ cần đúng kho + AWB 11 số +
  Người khai/Agent. Không bắt Ops HT, không tự Quét khi Điền.
- Đã bỏ gate `RECEPTION` trong `canFillEsidForPortal` và helper
  `ensureEsidFillReception`.

## QA Playwright MCP — chỉ kho TCS (Railway)

- Health/status TCS: `scope=TCS`, user `namnam8012` (không nhầm `hanam7195`).
- Hub song song: TECS-TCS = `hanam7195` — tách đúng port.
- Menu ⋮ → Điền trên Ops production vẫn lỗi gate cũ
  *«Chỉ điền lô đã Hoàn thành tiếp nhận — bấm Quét…»* → bản độc lập
  chưa deploy.
- `declare-fill` trực tiếp agent TCS AWB `18011226106` / KE0476:
  `ok:true`, chọn chuyến OK; `shpReg*` + `#agentId` + dest fail;
  lần sau có lúc rơi `NEEDS_LOGIN`.
