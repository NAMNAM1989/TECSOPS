/**
 * Cỡ chữ gốc (pt) — vùng tuyệt đối mm trong .lbl-sheet; slider % vẫn nhân (labelFontScale).
 * Giữ vừa LABEL_HEIGHT_MM = 80; tăng nhẹ nếu đổi sang tem 150 mm.
 */
export const LABEL_PT = {
  airlineLine: 14,
  awb: 22,
  caption: 7,
  airport: 15,
  pieces: 28,
} as const;
