# 2026-07-22

## Gom code trùng chức năng (merged PR #7)

- `esidDeclareFields` — nguồn chung Excel dry-run + Điền fill (payment = Chuyển khoản; HAWB thống nhất)
- `esidProfileStoreFactory` — Agent/Người khai dùng chung load/save/normalize
- `downloadXlsx` — blob download + `awbForFilename`
- `customerSavedListOps` — CRUD saved shipper/cnee/goods/vehicle
- `shared/customerProfileLimits.mjs` — limits 1 nguồn cho validate server + clamp client
- Xóa stub `downloadReady` trong `useTcsPortalActions`

## Wave 1 cleanup dead/legacy (merged PR #8)

- Gỡ prop `onOpenTcsPortal` + focus TCS dead (`tcsFocusShipment` / `clearFocusHint`)
- Xóa deprecated SCSC rounding aliases + `scscRuleLabel` + `dimKgExcelNumFmt`
- Xóa `splitScscAddress*` (chỉ test)
- Xóa alias Ecargo vehicle / `loadWorkDate`+`saveWorkDate` exports / `LegacyLabelPrintMode`
- Xóa nhánh `capRatio` 90% trong `dimBulkFill` + `mergePrinterProfileCatalogs`
- Tests: 284 passed

## Wave 2 (PR #9)

- `EsidProfileSettingsButton` — UI chung; Agent/Registrant thin wrapper
- `printHtmlViaHiddenIframe` — DIM SCSC + LIST DIM TCS
- `shared/airlineLabelOverridesNormalize.mjs`
- Fix: badge/title theo hồ sơ đã lưu

## Wave 3 (PR #10)

- Gỡ dead Ecargo helpers trong `customerVehicleCore`
- `shared/thermalLabelPresets.mjs`
- Chrome ext load `locators.json` + dual-try other_request (v1.1.3)

## Wave 4 (PR #11)

- `shared/esidProfilesNormalize` / `vehiclePlateNormalize` / `primitiveNormalize`
- Plate validation đếm alnum
- Python DEFAULT `other_request`

## Wave 5 integrate (merged PR #12)

- Branch gộp #9+#10+#11 (resolve conflict printer/ext/README)
- Python: `_field_id` + DEFAULT_LOCATORS đủ party fields; `other_request` dual-try shcOthReq|otherRequest
- Ext v1.1.3 giữ ensureLocators + dual-try
- Deploy Railway: `deploy:ship` + empty commit `9800980`; health `postgres:true`

## Kiểm tra toàn dự án (12:00 UTC)

- main `@9800980`; typecheck/lint OK; Vitest 279; CI main xanh
- Production health OK; PR mở còn #6 (noVNC review stub)
- Báo cáo đầy đủ giao user trong chat

## Wave 6 audit (shorten) — tip `11b3cea` / branch `cursor/cleanup-wave6-shorten-976b`

Audit only (no code change). SAFE-now highlights:
- Dead: `roundToStep`, `applyQrOneDecimalRule` (+ tests), `formatTotalDimKgLabel`, estimated note helpers, CNEE tooltip/clipboard, `labelFormatMismatch`, `createEmptyAgent/Registrant`, `closeDbPool`, `probeNovnc`
- Collapse: `customerVehicleCore` aliases, CD/`customerDirectoryStyles` barrel, `tcsAgentPdfUrl` thin wrap, `formatLineDimKgLabel`
- Skip: printer client/server unify, CNEE copy-block feature (unwired), `resolveScsc*` wiring vs delete, DIM 5000 empty list, Inline* merge, AirCargoTracking / Playwright / API auth

## Wave 6 implement

- Xóa dead + collapse theo audit: `customerVehicleCore`, CD barrel, `tcsAgentDocUrl`/`PdfUrl`, `resolveScscDimRuleFromFlight`, `closeDbPool`, `probeNovnc` (+ bỏ `import http` thừa), SCSC/CNEE/thermal/ESID empty helpers
- Modal khách → `OPS` / `opsInput` trực tiếp; biển số → `vehiclePlateNormalize`; search → `findCustomerEntry`

## Wave 7 audit + implement

- Dead: `parseDimLineQuadsFromNumbers`, `volumetricKgFromCm`, `parseKgInput`, `parsePositiveNumbersFromText`, `DIM_ROUNDING_POLICY_IDS`, `dimRoundingPolicyFromFlight`, `truncatePositiveKg` re-export, `fetchAppStateRows`, `createInitialState`, `clearSheetFetchCaches`, `isAutoWorkflowStatus` (src wrap), `resolveScsc*Print` (chỉ test)
- Collapse: `opsInput` → `OPS.input`, `statusRowBg` một const, bỏ barrel `filterCustomerDirectoryEntries` từ dropdown, unexport HQ helpers postgres
- Skip: CNEE copy-block display còn sống, printer unify, AirCargoTracking, API auth

## Deploy PR #13 (14:38 UTC)

- Merge #13 → main `4d80bc8`; `deploy:ship` + empty commit `4bdd421`
- Health: `https://ops-production-b405.up.railway.app/api/health` → `postgres:true`

## Fix kéo Sheet SCSC không về

**Triệu chứng:** Bấm Sheet chỉ thấy kéo về trang TCS; chọn SCSC thì không ở được / không kéo.

**Nguyên nhân:**
1. `filteredViewRows` rỗng → effect ép `activeWarehouse` về TCS → không đứng được trang SCSC trống để kéo Sheet.
2. Parser: dòng tiêu đề giữa khối (`VLC-TECS` / `CẬP NHẬT…`) xóa `colMap` → bỏ hết dòng SCSC phía sau.

**Sửa:** giữ kho khi list trống; modal Sheet lọc/ưu tiên theo kho đang xem; sau nhập chuyển đúng kho; parser không xóa colMap khi gặp title.

## Deploy PR #14 lên Railway

- Merge `cursor/fix-sheet-pull-scsc-0aa3` → `main` (68a0445) + empty commit trigger (6f32d5f).
- Health: `https://ops-production-b405.up.railway.app/api/health` → ok + postgres.
- Sheet sync 2026-07-22: 37 lô (TCS 23 · SCSC 14).

## Fix Sheet: SCSC không nhập (regression filter kho)

**Nguyên nhân:** Modal Sheet mặc định lọc + chọn theo `activeWarehouse` (thường TCS) → bấm Nhập chỉ gửi lô TCS, SCSC bị bỏ dù có trên Sheet.

**Sửa:** Mở modal luôn xem «Tất cả», chọn sẵn mọi lô nhập được (TCS+SCSC). Chip kho chỉ lọc danh sách xem, không cắt selection.

## Deploy PR #16 — Sheet nhập cả SCSC

- Merge + trigger `087e443` lên Railway.
- Health OK; mở Sheet mặc định chọn TCS+SCSC.

## Redeploy Railway (trigger)

- Empty commit `4638578` → GitHub → Railway production.
- Health: ops-production-b405.up.railway.app/api/health OK + postgres.

## Chrome extension — Điền ESID trên tab TCS thật

- Thêm `chrome-extension/` MV3: background + content-ops (bridge Ops) + content-tcs (điền form) + popup + `locators.json`.
- Ops: `src/utils/tcsChromeExtension.ts` — postMessage channel `tecsops-tcs-ext`.
- `fillEsidDeclareFor` ưu tiên extension; không `ensureSessionReady` / agent. Thiếu ext → hướng dẫn cài.
- Badge **Ext OK/thiếu**. Bỏ menu «Điền Playwright» — chỉ còn **Điền** (extension).
- Bỏ warning «Agent trên lô»; Excel AGENT_* lấy hồ sơ Agent ESID cố định.
- Docs: `chrome-extension/README.md`, `TOOLS.md`, cập nhật `tcs-awb-automation/README.md`.
- Verify: typecheck + 282 tests + build xanh. Checklist tay: Load unpacked → Login TCS → Điền → HOÀN TẤT trên TCS.

## Test cài extension (2026-07-22)

- Chrome 150: `--load-extension` CLI không đủ tin (branded); dùng CDP `Extensions.loadUnpacked` + `--enable-unsafe-extension-debugging`.
- `npm run ext:verify` → Load unpacked OK, popup «Sẵn sàng», PING/PONG, content-ops bridge trên `:5173` OK.
- Extension ID unpacked phụ thuộc path (vd. `ncalmohemjogcgbllbkhcnblijolagcl` khi load từ `d:\TECSOPS\chrome-extension`).

## Live test Điền ESID lô đầu (Playwright fallback)

- Cursor browser không load Chrome extension → badge **EXT THIẾU**; **Điền** fallback Playwright.
- Lỗi: `#shipperId` không chọn master cho tên PCS dài (gõ full name + khớp substring có dấu).
- Fix: `_fill_combobox` tìm token ngắn (`PCS`…) + so khớp bỏ dấu; extension `fillAntSelect` tương tự.
- Retest AWB `738-0725 3886`: điền OK ~16s, hết warning shipperId; panel **HOÀN TẤT trên TCS**.

## Extension treo UI (popup chồng modal chuyến bay)

- Hiện tượng: Điền → 3 card Shipper/Agent/CNEE (+ Thêm mới) chồng modal «Danh sách chuyến bay».
- Nguyên nhân: `fillAntSelect` chỉ tìm `.ant-select-item-option`, không click card master TCS; điền party trước rồi mở flight → overlay chồng.
- Fix `content-tcs.js`: thứ tự AWB→flight→dest→party; `collectMasterOptions` nhận card «+ Thêm mới»; click tên (không +Thêm mới); `dismissOverlays` giữa bước; Cancel modal nếu không chọn được chuyến.

## Extension v1.1.0 — vẫn treo

- Root cause phụ: `background` re-inject `content-tcs.js` mỗi lần lỗi → nhiều `onMessage` listener → FILL chạy song song → chồng popup.
- Fix: IIFE idempotent (`__TECSOPS_TCS_LISTENER__` + thay `runFill`); luôn inject trước FILL; `hardResetUi` đóng dropdown/modal; chọn master bằng ArrowDown+Enter; busy lock; Ops hiện `extension v1.1.0` để xác nhận đã Reload.
