# 2026-08-22

## ERROR_MONITOR_AGENT

Thiết kế + implement agent quan sát lỗi TECSOPS (nhánh `cursor/error-monitor-agent-1098`).

- Runtime: `src/agents/errorMonitor/` (mirror `src/agents/bugFix/`)
- Skill: `.agents/skills/error-monitor-agent/` (+ `.cursor/skills/`)
- CLI: `npm run error-monitor`
- Dispatcher: `ErrorMonitorEvent` → `bugReportFromMonitorEvent` → `runBugFixAgent`
- Hook Express 5xx + `/api/health` 503 (fail-isolated)
- Tests A–H trong `src/agents/errorMonitor/errorMonitorAgent.test.ts`

## BugFixAgent

Implemented production-grade BugFixAgent for TECSOPS:

- Module: `src/agents/bugFix/`
- Skill: `.agents/skills/bug-fix-agent/` (+ `.cursor/skills/`)
- CLI: `npm run bugfix:agent`
- Docs: `docs/bug-fix-agent.md`
- Vitest scenarios A–F pass (`src/agents/bugFix/bugFixAgent.test.ts`)

Constraints kept: no auto-commit of app fixes, no prod deploy, no auth weakening, never abbreviate Đăng Nhập TCS as ĐN.
