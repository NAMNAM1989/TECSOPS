# 2026-08-25

## Ảnh báo cáo lô — nút trên thanh công cụ

User muốn Vantage / Tecs / TCS / SCSC ra khỏi menu ⋯ (mobile production đang chôn 4 loại chụp ảnh báo cáo).

Nhánh `cursor/ops-cargo-report-toolbar-4a7a` từ `origin/main` @ `8075071`.

- Component mới `OpsCargoReportToolbar`: desktop hàng compact, mobile chip ≥44px cuộn ngang.
- Desktop: bỏ overflow «Báo cáo»; hàng Ảnh nằm giữa command row và DayPulse — không tranh `+ Booking`.
- Mobile: hàng Ảnh sau DayPulse; ⋯ chỉ còn Tải Ext / Công cụ (Sheet, Thống kê, Khách, Excel…).
- Logic copy ảnh / phạm vi kho giữ nguyên (`cargoDayReportImage`, `listOpsCargoReportActions`).
- Không đụng Ext protocol, auth, DIM, print, hash route.

## Hotfix — revert PR #72 (Ext / Đăng Nhập TCS / eCargo)

Railway live `83b839c` (~12:22 ICT) — page + `/api/state` + auth OK, nhưng Ext/eCargo gãy: `SET_ECARGO_SCSC_STORE` unknown action; 404 `/api/ecargo/vct`, `/api/tcs-extension`, `/api/tcs-extension/status`, `/api/chrome-extensions`, `/api/ecargo-extension`.

Nguyên nhân: PR #72 FF-merge `13b756e` + leftover `83b839c` (~−17k dòng).

Revert **chỉ #72** trên `cursor/revert-pr-72-7699` từ `origin/main` @ `83b839c`:
1. `git revert 83b839c` → `1ce048b`
2. `git revert 13b756e` → `8291fa0`

Cây `HEAD^{tree}` = `60253f1^{tree}`. #71 (ảnh báo cáo) vẫn ancestor. Không đụng PR #70. Không reset về `8075071`.

## Gỡ sạch Tải Ext / Đăng Nhập TCS / eCargo (PR mới, không reopen #72)

User xác nhận gỡ vĩnh viễn. Nhánh từ `origin/main` @ `b1c377d` (sau revert #73). **Không** revert #73, **không** reopen #72.

- Gỡ Ext TCS/SCSC, menu Tải Ext, `/api/chrome-extensions`, `/api/tcs-extension*`, thanh Đăng Nhập TCS.
- Gỡ eCargo UI + `/api/ecargo*` + mutation `SET_ECARGO_*`. Postgres chỉ strip key eCargo khi lưu blob — không drop DB / không đụng lot-customer.
- OCR Docker + IMAP chỉ phục vụ captcha Ext / OTP eCargo → gỡ. Dockerfile Node-only; không `ext:fetch-ocr` / `common.onnx`.
- Giữ: 4 mã kho, DayPulse, Stats tuần, densify, DIM #68, print, CSD, #71 ảnh báo cáo, Sheet, ESID local (`EsidSettingsMenu` trên thanh công cụ), AppAuthGate, Overview strip.

Khác #72: UI + API + mutation gỡ cùng lúc — không còn caller → không 404 / unknown action trên live.

## Phase 0 recovery — ext_tcs (docs-only)

Nhánh `cursor/ext-tcs-phase0-recovery-59c4` từ `main` @ `f2c8a5c`. **Không** restore `chrome-extension-tcs/` vào runtime app.

- Last tree: `b1c377d:chrome-extension-tcs/` (= `60253f1` folder Ext). MV3 vanilla JS v1.5.3, không React/Dexie.
- Docs: `docs/ext_tcs/{PROJECT_SPEC,ARCHITECTURE,AGENT_STATUS,DECISIONS,PHASE0_RECOVERY_REPORT}.md`.
- P0 Dexie origin-split loại. P0 thật: password/jar plaintext + host Ops đã mất (#74). OCR = ONNX leftover, không Tesseract; không nâng CAPTCHA.
- Check: `node --check` + JSON parse. Không live TCS.
