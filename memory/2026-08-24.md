# 2026-08-24
## P0 khôi phục import Google Sheet
- Re-enable `registerSheetsRoutes` + CTA «Nhập Sheet» (desktop command bar + mobile overflow).
- Restore `server/sheets/*` + modal/API utils từ pre-A3; gỡ phụ thuộc Gemini khỏi `GoogleSheetImportModal`.
- Sheet live probe 24-AUG: tab «NGÀY 24 AUG», parse OK.
- Chưa commit/deploy — chờ user.
