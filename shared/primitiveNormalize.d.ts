export declare function clamp(n: number, min: number, max: number): number;

export declare function str(v: unknown, max?: number): string;

export declare function num(
  v: unknown,
  fallback: number,
  min: number,
  max: number
): number;
