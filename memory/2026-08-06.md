# 2026-08-06

## eCargo Ext v2.2.8 — bugfix + speed

- Success giả / IMAP / verify — đã ship `99e590a`.

## Local probe form eCargo → Ext v2.2.10 / v2.2.11

Trên https://ecargo.scsc.vn/Export/VCTOrder/Create:

1. Autocomplete: menu `CODE - NAME - ADDRESS`, select → `item.name` + `item.val`.
2. **Hồ sơ Ops `NAM NAM LOGISTICS` KHÔNG tồn tại** trong API Customer Agent eCargo (0 kết quả). Gần: NAM NHAT / NAM SON / NAM VIET LOGISTICS…
3. `#btnCreate` jQuery + Turnstile + CheckArrivalDate (≥90 phút) + validateName (phải có khoảng) + OTO biển 7–9.
4. `addAWB` gọi GetExportFlight — chuyến không tồn tại thì không thêm dòng.

v2.2.11: preset AgentIdent/Code, auto slot ≥90p, bắt lỗi `#lbAwbError`/`#errorMessage`, radio bắt buộc tick.

## Live proof 2026-08-06 ~12:00

- Hồ sơ local vẫn `NAM NAM LOGISTICS` — API Agent eCargo **0 kết quả** (kể cả `CO LTD`).
- Điền tay + agent **NNL / NAM NHAT LOGISTICS** + 1 AWB → **Tạo phiếu OK** → `/Export/VCTOrder/Details/SDNDHZHP` (phiếu thử nghiệm lúc probe).
- Kết luận: app/ext tạo phiếu được; blocker = tên đại lý Ops không có trên eCargo.
- v2.2.12: `ECARGO_LOOKUP_AGENT` + preflight chặn đăng ký khi tên không khớp.

## Live E2E thành công ~12:25

- Đổi hồ sơ local → `NAM NHAT LOGISTICS CO.,LTD` + Ident `80928` / Code `NNL`.
- Điền + AWB `978-23967204` / VJ842 / TPE; ngày vào **07/08 08:00** (cùng ngày cut-off VJ842 fail).
- **Tạo phiếu OK** → `14IOH47H` → OTP mail → xác thực → Details barcode, status **Đang Xử Lý**.
- Bug IMAP: `imapflow.fetch([uid…])` trên Gmail trả 0 msg → OTP wait timeout dù mail đã có. Fix: fetch `{ since }` rồi chấm điểm. Restart API để nhận `agentIdent` + fix OTP.

## Root cause «NAM NAM LOGISTICS» + Ext v2.2.13

1. eCargo autocomplete `focus`/`select` **ghi đè** ô đại lý bằng `item.name` từ list Agent — list Agent **không có** NAM NAM (chỉ Shipper `CONG TY TNHH NAM NAM LOGISTICS` / 233031).
2. Ext/Ops cũ bắt buộc khớp Agent + Ident ≠ 0 → sai hướng (đổi sang NAM NHAT hoặc chặn).
3. **Chứng minh:** gõ tay `NAM NAM LOGISTICS` + Ident=`0` + tắt autocomplete → **Tạo phiếu OK** → `RLKLLNFR`, Đại lý = NAM NAM LOGISTICS.
4. Fix v2.2.13: disable autocomplete khi điền tên hồ sơ; bỏ preflight/AGENT_IDENT_MISSING; không thay tên bằng gợi ý.

## eCargo biển số bị xóa (XEMAY) → Ext v2.2.14

- eCargo `radVehicleType` click handler: `$("#txtVehicleNo").val("")` khi chọn OTO/XEMAY/BAGAC.
- Ext `clickCreateOrder` tick lại radio trước submit → xóa biển → lỗi «Biển số xe không được trống».
- Fix: không click lại radio nếu đã chọn đúng; `applyVehicleHeader` luôn gán biển sau radio.
- eCargo ghi lỗi vào `#valVehicle` (không phải ASP.NET) — Ext clear luôn `#valVehicle` + các `#val*`.
- Retest CDP 2026-08-06: bug clear plate=true; fix path plate kept + validateVehicle would pass + `#valVehicle` rỗng.
