# 2026-08-03

## eCargo SCSC — test thật + fix lỗi user báo

- User: ngày hàng vào sai (không chọn được khung) + họ tên không có khoảng trắng.
- Form eCargo thật: placeholder `NGUYEN VAN A` (có space); `#txtArrivalTime` value 0..23; ngày > hôm nay.
- Fix: `normalizeEcargoPersonName` giữ space; content-ecargo `setDateInput`/`setSelectValue` + ép ngày mai nếu ≤ hôm nay; modal ghi chú + min date.
- Package extension **v2.1.3** → `public/downloads/tecsops-chrome-extension-v2.1.3.zip`.
- Test Ops UI: modal có ngày 04/08 + khung 24 slot; NV `NGUYEN DUC TIN`, TX `HOANG CAO NHAN`.
- Cursor browser không có Ext → fill TIMEOUT (cần Chrome Reload Ext 2.1.3).
