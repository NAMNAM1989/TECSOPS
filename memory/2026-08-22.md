# 2026-08-22

## BugFixAgent

Implemented production-grade BugFixAgent for TECSOPS:

- Module: `src/agents/bugFix/`
- Skill: `.agents/skills/bug-fix-agent/` (+ `.cursor/skills/`)
- CLI: `npm run bugfix:agent`
- Docs: `docs/bug-fix-agent.md`
- Vitest scenarios A–F pass (`src/agents/bugFix/bugFixAgent.test.ts`)

Constraints kept: no auto-commit of app fixes, no prod deploy, no auth weakening, never abbreviate Đăng Nhập TCS as ĐN.
