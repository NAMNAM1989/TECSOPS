# 2026-08-25

## Ảnh báo cáo lô — nút trên thanh công cụ

Đã merge #71 + deploy Railway `60253f1`.

## Gỡ Tải Ext / Đăng Nhập TCS / eCargo

User xác nhận không dùng nữa. Nhánh `cursor/remove-ext-tcs-ecargo-4a7a`.

Gỡ:
- `chrome-extension-tcs/` + `chrome-extension-scsc/`
- Menu Tải Ext, thanh Cổng TCS, eCargo VCT/IMAP, Điền eSID qua Ext
- API `/api/chrome-extensions`, `/api/tcs-extension*`, `/api/ecargo*`
- Mutation `SET_ECARGO_*`; blob Postgres sẽ strip key eCargo khi lưu lại
- Dockerfile OCR/Python + deps `imapflow` / `mailparser` / `onnxruntime-web`

Giữ: 4 mã kho, DIM, in tem/CSD, báo cáo ảnh, Sheet, hồ sơ ESID (Người khai/Agent) trong Công cụ, Excel eSID utils, app login, workflow `RECEPTION_COMPLETED` (sửa tay).
